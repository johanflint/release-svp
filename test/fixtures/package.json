{
  "name": "test-package",
  "version": "1.2.2",
  "description": "fixture for PackageJson updater tests",
    "scripts": {
        "build": "tsc"
    },
  "config": {
    "version": "should-not-change"
  }
}
